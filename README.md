# ADB Studio (PyQt6)

GUI для ADB на PyQt6: управление Android-устройствами, установка APK,
file push/pull, logcat, live screen, root-помощник и многое другое.

## Установка

```bash
pip install -r requirements.txt
python main.py